@* ============================================================
   EmptyLayout.razor
   A bare-bones layout with no sidebar, nav, or header.
   Used by StudentSystem.razor via @layout EmptyLayout.

   PLACEMENT: Shared/EmptyLayout.razor
              (same folder as MainLayout.razor)
   ============================================================ *@

@inherits LayoutComponentBase

@* Render the page content directly — no wrapper, no nav *@
@Body